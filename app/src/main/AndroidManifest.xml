<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      This app is fully offline. It intentionally does NOT declare the INTERNET
      permission, so no tool can transmit anything off the device.
    -->

    <!-- Vibration Test tool. Normal permission, granted at install time. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Network Info tool reads the connection type and IP addresses. Normal permission. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- QR Scanner tool only. Requested at runtime, never used in the background. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Sound Meter tool only. Requested at runtime, never recorded or stored. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Everything degrades gracefully, so nothing here is required to install. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen.multitouch" android:required="false" />

    <!--
      Package visibility for the App Inventory tool. We deliberately avoid the
      restricted QUERY_ALL_PACKAGES permission and only resolve apps the user
      can actually launch.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".UtilityBoxApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UtilityBox"
        tools:targetApi="31">

        <!-- Lets the QR generator hand a rendered code to another app to share. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
          Home screen flashlight widget. Toggling the torch uses
          CameraManager.setTorchMode, which needs no camera permission.
          Exported because the platform delivers widget broadcasts to it.
        -->
        <receiver
            android:name=".widget.FlashlightWidgetProvider"
            android:exported="true"
            android:label="@string/widget_flashlight_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/flashlight_widget_info" />
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.UtilityBox">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
