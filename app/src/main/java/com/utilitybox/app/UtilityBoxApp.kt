package com.utilitybox.app

import android.app.Application

/**
 * Application entry point. Deliberately empty of any network, analytics or
 * tracking initialisation: every tool in this app runs entirely on-device.
 */
class UtilityBoxApp : Application()
